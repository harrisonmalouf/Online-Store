<div class="jumbotron">
  <div class="container-fluid">
    <h1 class="display-4">Welcome to the Home Page</h1>
  </div>


<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <%= image_tag("Xbox-console-light.jpg", :class => "d-block w-100") %>
    </div>
    <div class="carousel-item">
      <%= image_tag("xbox-dark-background.jpg", :class => "d-block w-100") %>
    </div>
    <div class="carousel-item">
      <%= image_tag("xbox-dark-photo.jpg", :class => "d-block w-100") %>
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
<!-- --------------------------------------------------------------------- -->
<div class="container">
  <% @game.each_slice(4) do |game| %>
  <div class="row">
    <!--TAKE THIS -->
    <div class="col-md-3">
      <div class="card reset">
        <div class="img-wrap"><%=  image_tag(game[0].image, :class => "card-img-top") %></div>
        <div class="card-body">
          <h4 class="card-title title"><%= game[0].name %></h4>
          <p class="card-text"></p>
        </div>
        <ul class="list-group list-group-flush">
          <li class="list-group-item price">Price: <%= game[0].price %></li>
          <li class="list-group-item stock">Stock:  <%= game[0].stock %></li>
        </ul>
        <div class="card-body">
          <% if @current_user.present? %>
            <a href="<%= game_path(game[0].id) %>" class="btn btn-primary">More Information</a>
            <% else %>
            <a href="<%= login_path %>" class="btn btn-primary">More Information</a>
            <% end %>
        </div>
        <div class="card-body">
          <% if @current_user.present? %>
            <% if game[0].stock> 0  %>
              <a href="<%= new_order_path(:game => game[0].id) %>" class="btn btn-success">Purchase</a>
            <% else %>
              <p class="red">Out of stock<p>
            <% end %>
          <% else %>
            <a href="<%= login_path %>" class="btn btn-success">Purchase</a>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Column 2: -->
    <div class="col-md-3">
      <div class="card reset">
        <div class="img-wrap"><%=  image_tag(game[1].image, :class => "card-img-top") %></div>
        <div class="card-body">
          <h4 class="card-title title"><%= game[1].name %></h4>
          <p class="card-text"></p>
        </div>
        <ul class="list-group list-group-flush">
          <li class="list-group-item price">Price: <%= game[1].price %></li>
          <li class="list-group-item stock">Stock:  <%= game[1].stock %></li>
        </ul>
        <div class="card-body">
          <% if @current_user.present? %>
            <a href="<%= game_path(game[1].id) %>" class="btn btn-primary">More Information</a>
          <% else %>
            <a href="<%= login_path %>" class="btn btn-primary">More Information</a>
          <% end %>
        </div>
        <div class="card-body">
          <% if @current_user.present? %>
            <% if game[1].stock> 0  %>
              <a href="<%= new_order_path(:game => game[1].id) %>" class="btn btn-success">Purchase</a>
            <% else %>
              <p class="red">Out of stock<p>
            <% end %>
            <% else %>
            <a href="<%= login_path %>" class="btn btn-success">Purchase</a>
          <% end %>
        </div>
      </div>
    </div>
  <!-- Column 3: -->
  <div class="col-md-3">
    <div class="card reset">
      <div class="img-wrap"><%=  image_tag(game[2].image, :class => "card-img-top") %></div>
      <div class="card-body">
        <h4 class="card-title title"><%= game[2].name %></h4>
        <p class="card-text"></p>
      </div>
      <ul class="list-group list-group-flush">
        <li class="list-group-item price">Price: <%= game[2].price %></li>
        <li class="list-group-item stock">Stock:  <%= game[2].stock %></li>
      </ul>
      <div class="card-body">
        <% if @current_user.present? %>
          <a href="<%= game_path(game[2].id) %>" class="btn btn-primary">More Information</a>
          <% else %>
          <a href="<%= login_path %>" class="btn btn-primary">More Information</a>
          <% end %>
      </div>
      <div class="card-body">
        <% if @current_user.present? %>
          <% if game[2].stock> 0  %>
            <a href="<%= new_order_path(:game => game[2].id) %>" class="btn btn-success">Purchase</a>
          <% else %>
            <p class="red">Out of stock<p>
          <% end %>
        <% else %>
          <a href="<%= login_path %>" class="btn btn-success">Purchase</a>
        <% end %>
      </div>
    </div>
  </div>

<!-- Column 4: -->
<div class="col-md-3">
  <div class="card reset">
    <div class="img-wrap"><%=  image_tag(game[3].image, :class => "card-img-top") %></div>
    <div class="card-body">
      <h4 class="card-title title"><%= game[3].name %></h4>
      <p class="card-text"></p>
    </div>
    <ul class="list-group list-group-flush">
      <li class="list-group-item price">Price: <%= game[3].price %></li>
      <li class="list-group-item stock">Stock:  <%= game[3].stock %></li>
    </ul>
    <div class="card-body">
      <% if @current_user.present? %>
        <a href="<%= game_path(game[3].id) %>" class="btn btn-primary">More Information</a>
      <% else %>
        <a href="<%= login_path %>" class="btn btn-primary">More Information</a>
      <% end %>
    </div>
    <div class="card-body">
      <% if @current_user.present? %>
        <% if game[3].stock> 0  %>
        <a href="<%= new_order_path(:game => game[3].id) %>" class="btn btn-success">Purchase</a>
        <% else %>
        <p class="red">Out of stock<p>
        <% end %>
        <% else %>
        <a href="<%= login_path %>" class="btn btn-success">Purchase</a>
      <% end %>
    </div>
  </div>
</div>




  </div>
  <% end %>
</div>
</div>
